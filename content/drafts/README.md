# drafts/ 草稿区

存放正在写、还没发布的内容。

## 使用方式

1. 从 `templates/article.md` 复制一份到这里
2. 填写内容
3. 写好后改 frontmatter 的 `status` 为 `published`
4. 移到 `content/articles/YYYY/MM/` 目录
5. 更新 `index.html` 的 newsData 数组

## 命名建议

- `YYYY-MM-DD-标题简写.md`(写完直接移走,改不改名都行)
- 或 `标题简写.md`(还没定日期时)
